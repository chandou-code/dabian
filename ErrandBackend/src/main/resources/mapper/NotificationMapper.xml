<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.errand.mapper.NotificationMapper">

    <!-- 获取用户通知列表 -->
    <select id="getUserNotifications" resultType="com.campus.errand.entity.Notification">
        SELECT id, user_id, type, title, content, tag_name, task_id, notice_id, `read`, created_at, read_at
        FROM notifications
        WHERE user_id = #{userId}
        <if test="type != null and type != ''">
            AND type = #{type}
        </if>
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 获取用户未读通知数量 -->
    <select id="getUnreadCount" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM notifications
        WHERE user_id = #{userId} AND `read` = 0
    </select>

    <!-- 标记通知为已读 -->
    <update id="markAsRead">
        UPDATE notifications
        SET `read` = 1, read_at = NOW()
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <!-- 标记所有通知为已读 -->
    <update id="markAllAsRead">
        UPDATE notifications
        SET `read` = 1, read_at = NOW()
        WHERE user_id = #{userId} AND `read` = 0
    </update>

    <!-- 清除所有通知 -->
    <delete id="clearAll">
        DELETE FROM notifications
        WHERE user_id = #{userId}
    </delete>

    <!-- 批量插入通知 -->
    <insert id="insertBatchSomeColumn" parameterType="java.util.List">
        INSERT INTO notifications
        (user_id, type, title, content, tag_name, task_id, notice_id, `read`, created_at, read_at)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.type}, #{item.title}, #{item.content}, #{item.tagName}, #{item.taskId}, #{item.noticeId}, #{item.read}, #{item.createdAt}, #{item.readAt})
        </foreach>
    </insert>

</mapper>